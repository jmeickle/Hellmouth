SHORT TERM GOALS:
- Basic combat
- Basic movement AI
- Basic combat AI
- Fill out lifepaths so all reachable events are meaningful

LONGER TERM GOALS:
- Basic skills
- Basic items

MISC:

DISPLAY:
- Support curses attributes
- Convert colors to default to a black background.
- Support 256 color mode.

UTILITY:
- Make everything using dirs use dir[NW] etc. rather than importing dirs separately.
- Make all pos functions accept tuple args consistently.
- Write a wrapper so that curses never, ever tries to print something offscreen.

STRUCTURE:
- Clean up the game loop
- Clean up the chargen screen. Too much of it is in the view.
- Split into a folder structure.


